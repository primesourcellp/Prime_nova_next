'use client';
import { useState, useEffect } from 'react';
import type { FormEvent } from 'react';
import { Download, CheckCircle2 } from 'lucide-react';
export default function ContactForm(){const [interest,setInterest]=useState('Custom software development');const [done,setDone]=useState(false);useEffect(()=>{const value=new URLSearchParams(window.location.search).get('interest');if(value&&['0','1','2'].includes(value))setInterest(['Workforce management','Recruitment management','Property management'][Number(value)])},[]);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);const content=['PRIMENOVA — PROJECT BRIEF','',...Array.from(data,([k,v])=>`${k[0].toUpperCase()+k.slice(1)}: ${v}`)].join('\n\n');const url=URL.createObjectURL(new Blob([content],{type:'text/plain;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download='Primenova-project-brief.txt';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);setDone(true)}return <form onSubmit={submit} className="contact-form"><div className="grid gap-x-5 sm:grid-cols-2"><label>Your name<input name="name" autoComplete="name" placeholder="Alex Morgan" required/></label><label>Work email<input name="email" type="email" autoComplete="email" placeholder="you@company.com" required/></label></div><label>Company<input name="company" autoComplete="organization" placeholder="Your company name"/></label><label>What are you interested in?<select name="interest" value={interest} onChange={e=>setInterest(e.target.value)}>{['Custom software development','Web & mobile applications','Product engineering','Cloud & integration','Workforce management','Recruitment management','Property management'].map(i=><option key={i}>{i}</option>)}</select></label><label>Tell us about your idea<textarea rows={5} name="message" placeholder="What would you like to build or improve?" required/></label><button type="submit" className="button">Download project brief <Download size={17}/></button><p className="mt-4 text-xs leading-relaxed text-muted">Creates a text file for you to share. Your details are not submitted or stored.</p><div role="status" className="mt-4 text-sm text-deep-teal">{done&&<span className="flex gap-2"><CheckCircle2 size={18}/>Your brief is ready. Keep the downloaded file to share with the team.</span>}</div></form>}
