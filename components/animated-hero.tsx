'use client';
import { useRef, useState } from 'react';
import type { CSSProperties, PointerEvent } from 'react';
import { Pause, Play, MoveUpRight } from 'lucide-react';
export default function AnimatedHero(){const scene=useRef<HTMLDivElement>(null);const [paused,setPaused]=useState(false);function tilt(e:PointerEvent<HTMLDivElement>){if(e.pointerType==='touch'||window.matchMedia('(prefers-reduced-motion: reduce)').matches||paused)return;const rect=e.currentTarget.getBoundingClientRect();scene.current?.style.setProperty('--rotate-x',`${-(e.clientY-rect.top-rect.height/2)/45}deg`);scene.current?.style.setProperty('--rotate-y',`${(e.clientX-rect.left-rect.width/2)/45}deg`)}function reset(){scene.current?.style.setProperty('--rotate-x','0deg');scene.current?.style.setProperty('--rotate-y','0deg')}return <div className={`hero-scene ${paused?'is-paused':''}`} onPointerMove={tilt} onPointerLeave={reset} ref={scene} style={{'--rotate-x':'0deg','--rotate-y':'0deg'} as CSSProperties}><div className="hero-tilt"><div className="hero-float"><img src="/images/primenova-3d.webp" width="1100" height="1100" fetchPriority="high" alt="3D mobile app with floating teal glass dashboard tiles and amber accents" className="hero-image"/></div></div><div className="hero-tag hero-tag-top"><span className="tag-icon"><MoveUpRight size={18}/></span><span>From idea to impact<small>Thoughtfully engineered</small></span></div><div className="hero-tag hero-tag-bottom"><span className="tag-accent">✳</span><span>Made for what’s next<small>Designed around you</small></span></div><button className="motion-toggle" type="button" aria-pressed={paused} onClick={()=>{setPaused(!paused);reset()}} aria-label={paused?'Play hero animation':'Pause hero animation'}>{paused?<Play size={13}/>:<Pause size={13}/>}<span>{paused?'Play motion':'Pause motion'}</span></button></div>}
