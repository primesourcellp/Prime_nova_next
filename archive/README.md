# Archive

The newer software-company Primanova pages were removed from `app/` so the original recruitment product site under `src/app` could run again.

Related files that may still exist at the project root:
- `components/` (header, animated-hero, ui, etc.)
- `lib/content.ts`

To bring the software site back later, restore those pages into `app/` (or a separate branch) and point `@/*` at the project root.
